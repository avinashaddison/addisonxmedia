# AddisonX Media - Environment Variables Template
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env file to version control!

# ===========================================
# MySQL Database Configuration
# ===========================================
# External MySQL database hosted at storage2300.is.cc
MYSQL_HOST=your_mysql_host_here
MYSQL_PORT=3306
MYSQL_USER=your_mysql_username_here
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_DATABASE=your_database_name_here

# ===========================================
# Session Management
# ===========================================
# Secret key for session encryption (generate with: openssl rand -base64 32)
SESSION_SECRET=your_session_secret_here

# ===========================================
# Replit Authentication (OIDC)
# ===========================================
# Replit OIDC issuer URL for admin authentication
ISSUER_URL=https://replit.com

# ===========================================
# Replit Environment
# ===========================================
# Automatically set by Replit
REPL_ID=your_repl_id_here

# ===========================================
# Object Storage (Google Cloud Storage)
# ===========================================
# Private directory for user-uploaded files
PRIVATE_OBJECT_DIR=.private

# Public search paths for assets (comma-separated)
PUBLIC_OBJECT_SEARCH_PATHS=public

# Default object storage bucket ID
DEFAULT_OBJECT_STORAGE_BUCKET_ID=your_bucket_id_here

# ===========================================
# Production Database (Optional - Read Only)
# ===========================================
# PostgreSQL database URL (if using Replit's built-in database)
# DATABASE_URL=postgresql://user:pass@host:port/database

# ===========================================
# Notes
# ===========================================
# 1. All sensitive values above should be stored in Replit Secrets
# 2. You can access secrets via the Secrets tab in the Replit UI
# 3. Replit automatically injects secrets as environment variables
# 4. The .env file is only needed for local development outside Replit
